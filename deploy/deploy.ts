// ─── Deploy Service ───
// Types are re-exported for external consumers.
// Endpoints are auto-discovered by Encore from the endpoints/ directory.
export type { DeployEvent, Deployment, ProviderResponse } from "./shared";
