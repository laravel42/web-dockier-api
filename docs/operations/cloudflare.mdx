# Cloudflare Static Hosting

The frontend can be deployed as static assets to Cloudflare Pages.

## Build command

`pnpm --filter frontend build`

## Output directory

`frontend/dist`

## Config

See `frontend/wrangler.toml` for Pages-compatible local/deploy settings.
