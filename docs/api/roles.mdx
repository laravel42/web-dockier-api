# Roles API

## Endpoints

- `POST /roles`
- `GET /roles`
- `GET /roles/:roleId`
- `PUT /roles/:roleId`
- `DELETE /roles/:roleId`

## Notes

- Role records are scoped by authenticated `appId`.
- Permissions are represented as a string array.
