# Users API

## Endpoints

- `POST /users`
- `GET /users/:userId`
- `GET /users`
- `PUT /users/:userId`
- `DELETE /users/:userId`

## Query behavior

`GET /users` supports `page`, `limit`, and `search` query params with typed coercion and bounds.
