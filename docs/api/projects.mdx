# Projects API

## Endpoints

- `POST /projects`
- `GET /projects/:projectId`
- `GET /projects`
- `PUT /projects/:projectId`
- `DELETE /projects/:projectId`

## Config conventions

- `config` is stored as JSON and validated with strict schema limits.
- Post-deploy commands are capped and validated for command length and timeout.
