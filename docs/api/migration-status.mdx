# Migration Status Endpoints

Each scaffolded service exposes:

- `GET /<service>/_migration/status`

This endpoint is retained for migration observability and now reports migrated status.

## Migrated services

- `roles`
- `deploy`
- `notifications`
- `integrations`
- `code-analysis`
- `git-integration`
- `image-builder`
